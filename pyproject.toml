[project]
name = "px-manager"
version = "0.1.0"
description = "Proxy info manager"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "click>=8.4.1",
    "tornado>=6.5.6",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "pyinstaller>=6.20.0",
]
